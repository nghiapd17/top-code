import React from "react";
import useStyles from "./HomePage.style";

const HomePage = () => {
  const classes = useStyles();
  return <div className={classes.text}>HomePage</div>;
};

export default HomePage;
